#ifndef TYPES_HPP
# define TYPES_HPP

# ifndef __USE_GNU
#  define __USE_GNU
# endif // __USE_GNU

# include <cstdlib>

class							Debugee;
typedef size_t					t_word;
typedef size_t					t_program_ptr;
typedef unsigned long long int	t_reg;
#endif // TYPES_HPP
